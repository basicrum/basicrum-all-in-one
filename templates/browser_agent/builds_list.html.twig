<h4>Builds:</h4>
<hr />
<div class="list-group">
    {% if builds is empty %}
    <div class="list-group-item list-group-item-action list-group-item-warning">No builds yet :(</div>
    {% else %}
        {% for build in builds %}
        <a class="build-show-link list-group-item list-group-item-action list-group-item-light" href="{{ path('browser_agent_builds_show', {build_id : build.getId()}) }}">
            <span class="badge badge-info">{{ build.getId() }}</span> {{ build.getCreatedAt()|date("m/d/Y") }}
        </a>
        {% endfor %}
    {% endif %}
</div>