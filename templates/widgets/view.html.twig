<h4>{{ item.name }}</h4>
<p>
    Created by: {{ item.user }}
</p>
<div id="widget-data-container"></div>

<!-- Modal -->
{# <div class="modal fade" id="widget_view" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="modalWidgetLabel">Modal title</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">

            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div> #}

<script>
    document.addEventListener('DOMContentLoaded', function() {
       // alert("Ready!");
        $(function(){
        $.ajax('/widget/generate_diagram',{
            method: 'post',
            data: JSON.parse('{{ item.widget|raw }}'),
            success : function(response) {
                Plotly.react('widget-data-container', response.diagrams, response.layout, {displayModeBar: false, responsive: true})
            }
        });
    });
    }, false);

</script>